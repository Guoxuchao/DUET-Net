# DUET-Net (基于半监督的小目标计数项目) 文件结构详解

这份文档专门为你（深度学习初学者）准备。在进行接下来的科研任务（引入PPA模块、优化遮蔽策略、引入不确定性估计等）时，你可以随时参考这份文档，清楚地知道**“如果要改某个功能，应该去哪个文件里找代码”**。

---

## 1. 核心运行与入口文件 (Root Directory)
这些是直接在终端中运行的脚本，相当于整个项目的“开关”和“工具箱”。

*   **`train.py`**
    *   **作用**：**整个项目的总指挥部**。它是模型训练的入口文件。
    *   **里面有什么**：包含了命令行参数解析（比如设置学习率 `--lr`、训练轮数 `--max-epoch`、选择哪个数据集等）。它负责把数据集、模型、优化器、损失函数等各个部件组装起来，然后交给 `trainer` 开始训练。
    *   **何时修改**：当你需要新增一个训练参数（比如新增一个控制“不确定性估计”阈值的参数），或者想要更改默认参数时，需要修改这里。
*   **`mat_to_npy_converter.py`**
    *   **作用**：**数据格式转换工具**。
    *   **里面有什么**：将原始数据集中的 `.mat` 文件（通常是MATLAB格式的真实密度图标注）转换成 Python 更容易读取且速度更快的 `.npy`（NumPy数组）格式。
*   **`test_resize.py`**
    *   **作用**：用于测试图像或者调整图像尺寸的辅助脚本。
*   **`requirements.txt` / `get_requirements.py`**
    *   **作用**：记录了当前项目依赖的各种第三方库（如 PyTorch, torchvision, numpy 等）。

---

## 2. 模型架构定义 (`models/` 目录)
这里是你存放“大脑”的地方，所有的神经网络结构都在这里定义。**这是你进行“特征提取阶段创新（引入PPA模块）”的主战场。**

*   **`models/model_ppa.py`**
    *   **作用**：**核心模型文件**。
    *   **里面有什么**：定义了你的计数模型架构。从代码中可以看到，这里包含了你计划创新的 `VGG19` 前端（用于提取基础特征），以及后续的 `FPN`（特征金字塔，用于融合多尺度特征）和 `PPA`（包含了空间注意力机制、局部-全局注意力 `LocalGlobalAttention` 等新颖模块）。
    *   **科研任务关联**：当你需要**详细了解输入输出尺寸**、或者**新建带有 `Uncertainty` 前缀的新模型类**时，你需要在这个文件夹下操作。切记：不要破坏原有类，而是复制一份重命名后进行修改。

---

## 3. 训练与核心策略逻辑 (`utils/` 目录)
这里定义了模型“如何学习”。**这是你进行“遮蔽策略创新”和“不确定性估计创新”的主战场。**

*   **`utils/trainer.py` & `utils/regression_trainer.py`**
    *   **作用**：**训练引擎（发动机）**。
    *   **里面有什么**：包含了详细的训练循环（`train`）和验证循环（`validate`）。更重要的是，**半监督学习的核心逻辑全在这里**。例如：Teacher模型如何指导Student模型、**伪标签（Pseudo-label）的生成**、**遮蔽策略（Masking Strategy）的实施**、以及指数移动平均（EMA）的更新。
    *   **科研任务关联**：
        1.  **遮蔽策略创新**：你需要在这里找到 MRC 原有的 mask 生成代码，并进行你的微创新。
        2.  **不确定性估计（Uncertainty Estimation）**：在生成伪标签的过程中，你需要在这里引入计算不确定性的公式和代码，用于筛选或加权伪标签。
*   **`utils/helper.py`**
    *   **作用**：存放一些杂项的辅助工具函数（比如保存模型权重、计算误差指标如 MAE/RMSE 等）。
*   **`utils/logger.py`**
    *   **作用**：日志记录器。负责把训练过程中的损失（Loss）、误差（MAE/RMSE）等信息打印到终端，并保存到 `.log` 文件中。

---

## 4. 数据处理与加载 (`datasets/` 目录)
这里负责把硬盘里的图片“喂”给模型。

*   **`datasets/maize_semi.py`**
    *   **作用**：**玉米穗数据集的“装载机”**。
    *   **里面有什么**：继承自 PyTorch 的 `Dataset` 类。负责读取玉米图片、对应的真实密度图（Ground Truth），并对图片进行数据增强（Data Augmentation，比如随机裁剪、翻转等操作）。
    *   **科研任务关联**：因为玉米穗具有“小目标密集且穗偏移方向不同呈现多种形态”的特征，如果未来你需要加入特定的图像增强手段（比如针对不同形态的旋转），可以在这里修改。

---

## 5. 损失函数评估 (`losses/` 目录)
这里定义了用来衡量“模型猜得准不准”的尺子。

*   **`losses/losses.py`**
    *   **作用**：**损失函数大本营**。
    *   **里面有什么**：包含模型训练时用到的各种数学损失计算公式。比如均方误差（MSE Loss）用于全监督部分的密度图回归，以及半监督学习中常用的各种一致性损失（Consistency Loss）。
    *   **科研任务关联**：当你在伪标签阶段引入**不确定性估计**后，可能需要结合不确定度来重新设计损失函数（比如：不确定度高的地方，损失权重降低）。如果有新的损失函数公式推导，代码将写在这里。
*   **`losses/post.py`**
    *   **作用**：后处理代码。通常用于在计算损失前或计算完毕后，对输出结果做进一步的处理（比如平滑、阈值过滤等）。

---

## 6. 数据集划分清单 (`label_list/` 目录)
*   **`label_list/DUET_10.txt`, `DUET_20.txt`, `DUET_50.txt` 等**
    *   **作用**：**半监督数据划分名单**。
    *   **里面有什么**：由于是半监督学习，我们只有少部分数据有标注，大部分数据没有标注。这些 `.txt` 文件记录了不同比例下（比如10%有标签，50%有标签）具体使用了哪些图片的名字。模型在训练时会读取这些文件，从而知道哪些图片走“有监督学习”流程，哪些图片走“无监督伪标签”流程。

---

## 💡 科研创新下一步行动指南 (For Your Research)

根据你的需求，你接下来的操作顺序应当是：

1.  **深入理解模型结构 (针对小白)**：我会为你创建一个专门的脚本（例如 `model_inspector.py`），去加载 `models/model_ppa.py`，并将每一层卷积的输入输出尺寸、通道数、特征图大小详细打印出来并写入 MD 文件。
2.  **特征提取创新 (PPA模块)**：在 `models/` 下新建代码类（记得带上 `Uncertainty` 前缀），在现有 VGG19 基础上融合 FPN 和 PPA，不动原来的类。
3.  **遮蔽策略与不确定性估计创新**：主战场在 `utils/regression_trainer.py` 和 `losses/losses.py`。我们会在那里详细解释苹果树物候感知论文中的不确定性估计公式，并将其转化为代码逻辑。

按照这个文件结构，你的代码将保持非常清晰和模块化！